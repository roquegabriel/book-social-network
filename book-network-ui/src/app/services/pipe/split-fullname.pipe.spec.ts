import { SplitFullnamePipe } from './split-fullname.pipe';

describe('SplitFullnamePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitFullnamePipe();
    expect(pipe).toBeTruthy();
  });
});
